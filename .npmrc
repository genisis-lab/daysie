# better-auth-cloudflare 0.3 declares the pre-v5 Workers type range even though
# the runtime integration is compatible. Keep installs reproducible until its
# peer range catches up with Wrangler 4.112.
legacy-peer-deps=true
